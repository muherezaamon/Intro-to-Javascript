for(j=1; j<=10; j++)//for loop to iterate the count 10 times
    {
        
        let counter = prompt('Enter number:');//promts to enter number
     if (counter>0)
    
          {
           console.log(1); //outputs number is greater than zero
          }
       else if (counter<0)
          {
               console.log(-1); //outputs number if less thn zero
          }
    
        else
        {
          console.log(0);// outputs a zero if number is zero
        }
