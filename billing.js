//this is the percentage that determines the transaction charges
const transactioncharges=0.025;
var amountTendered=100000;
console.log("this is what the billed amount is(less transaction charges)");
console.log(amountTendered-(amountTendered*transactioncharges));
